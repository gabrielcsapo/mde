# @mde/plugin-sdk

Stable, versioned data contracts for MDEditor plugins. The SDK intentionally has no
renderer dependency: plugins request small capabilities and apply atomic transactions
instead of reaching into a platform editor implementation.
