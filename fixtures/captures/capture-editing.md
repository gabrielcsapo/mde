# Rendered until you edit it

Move the caret into **revealed syntax** and its delimiters return in place.

The rest stays rendered: *emphasis*, `code`, [links](https://example.dev), and @gabe.

The backing document is still this exact Markdown string on every platform.
