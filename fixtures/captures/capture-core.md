# CommonMark, rendered everywhere

Nested ***strong emphasis***, `inline code`, and a [real link](https://example.dev).

> One Rust core. Four native editing surfaces.

- [x] Headings and thematic structure
- [x] Ordered, unordered, and task lists
- [x] ~~Strikethrough~~ plus escaped \*literal markers\*

```rust
let renderer = Platform::Native;
```
