# Portable source forms

Escaped \*markers\* and an &copy; entity stay exact.

A soft break stays a newline,
while this line has a hard break.  
The next line starts here.

Raw <kbd>HTML</kbd> is highlighted, never executed.

<section data-kind="raw">
HTML blocks remain safe source.
</section>
